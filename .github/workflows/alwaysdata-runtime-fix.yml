name: Disabled obsolete alwaysdata runtime fix

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Obsolete runtime-fix workflow disabled. Deployment API tokens are never revoked automatically."
