name: One-shot alwaysdata deploy

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary encrypted handoff workflow disabled after successful live deploy."
