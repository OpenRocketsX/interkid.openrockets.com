name: Hotpatch Interkid verification route

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary alwaysdata verification hotpatch disabled after successful deployment."
