# Public URL of your deployed SearXNG instance.
SEARXNG_BASE_URL=https://interkid.openrockets.com/

# Generate once with: openssl rand -hex 32
# The container will generate an ephemeral secret automatically if this is blank.
SEARXNG_SECRET=

# Host port used by Docker Compose.
SEARXNG_PORT=8080
